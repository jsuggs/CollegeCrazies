{% extends "SofaChampsSquaresBundle::layout.html.twig" %}

{% block content %}
    <h2>Invite</h2>

    <form action="{{ path('squares_invite_send', {gameId: game.id}) }}" method="post">
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary btn-lg">Send Invites</button>
    </form>
{% endblock %}
