Hello {{ emailTo }},
Evidentially you are awesome
Why? Because {{ user.firstName|length ? user.firstName : user.username }} wants you to join them in a <a href="http://sofachamps.com">SofaChamps</a> league to play College Bowl Pick'em.

To accept the invitation, just <a href="{{ url('league_prejoin', {leagueId: league.id, season: season, rid: user.id}) }}">Click here</a> or copy and paste "{{ url('league_prejoin', {leagueId: league.id, season: season}) }}" into your browser and enter the League's information.
 * League Name: {{ league.name }}
 * League ID: {{ league.id }}
{% if not league.public %}
 * League Password: {{ league.password }}
{% endif %}
