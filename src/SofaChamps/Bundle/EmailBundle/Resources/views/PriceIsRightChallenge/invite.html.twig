{% extends 'SofaChampsEmailBundle:Layout:base.html.twig' %}

{% block content %}
    <p>{{ fromUser.username }} has invited you to play <i>Sofachamps Price Is Right Challenge</i>.</p>

    <p>To accept the invitation, just <a href="{{ url('pirc_join', {season: game['season'], gameId: game['id'], rid: fromUser['id']}) }}">click here to join.</a></p>

    <dl>
        <dt>Game Name</dt>
        <dd>{{ game['name'] }}</dd>
    </dl>
{% endblock %}
